---
phase: 1
plan: 01-01
title: "Limpar .env.example + Rotacionar Credenciais"
status: pending
wave: 1
depends_on: []
requirements: [SEC-01]
autonomous: false
files_modified:
  - ".env.example"
  - ".env"
  - ".gitignore"
summary: "Remover credenciais reais do .env.example e rotacionar todas as chaves expostas"
---

# 01-01: Credenciais Expostas no Git

## Goal
Remover todas as credenciais reais do `.env.example` (atualmente commitado com senhas/keys de producao), substituir por placeholders, e orientar rotacao das chaves comprometidas.

## MUST verify (blocking)
- [ ] `.env.example` contem APENAS placeholders (ex: `DB_PASS=SUA_SENHA_AQUI`)
- [ ] Nenhuma credencial real visivel no arquivo
- [ ] `.gitignore` protege `.env` e `.log` e `.env.worker`

## Tasks

### T01: Limpar .env.example

<read_first>
- `.env.example` — verificar o estado atual das credenciais expostas
- `.gitignore` — verificar regras atuais
</read_first>

<action>
Alterar `.env.example`:
- Substituir TODOS os valores reais por placeholders descritivos:
  - DB_HOST=SEU_DB_HOST
  - DB_NAME=SEU_DB_NAME
  - DB_USER=SEU_DB_USER
  - DB_PASS=SUA_SENHA
  - EVOLUTION_API_KEY=SUA_API_KEY_EVOLUTION
  - WEBHOOK_SECRET=SEU_WEBHOOK_SECRET (valor DIFERENTE do EVOLUTION_API_KEY)
  - OPENROUTER_API_KEY=SUA_API_KEY_OPENROUTER
  - GOOGLE_OAUTH_CLIENT_ID=SEU_CLIENT_ID
  - GOOGLE_OAUTH_CLIENT_SECRET=SEU_CLIENT_SECRET
  - SMTP_PASS=SUA_SENHA_SMTP
  - ABACATEPAY_API_KEY=SUA_API_KEY_ABACATEPAY
  - ABACATEPAY_WEBHOOK_QUERY_SECRET=SEU_SECRET
  - MAIL_FROM_EMAIL=seu@email.com
  - APP_BASE_URL=https://seudominio.com
- Remover IP real (212.85.3.45), nomes de DB reais, etc.
- Adicionar comentario avisando que sao placeholders

Nao alterar `.env` (esse tem as credenciais reais e esta no .gitignore).
</action>

<acceptance_criteria>
- `grep -c "212.85.3.45" .env.example` retorna 0
- `grep -c "HqNbkNK6geOrfwSvS0QHLLvEZrPrjdFa" .env.example` retorna 0
- `grep -c "sk-or-v1-" .env.example` retorna 0
- `grep -c "GOCSPX-" .env.example` retorna 0
- `grep -c "abc_prod_" .env.example` retorna 0
- `grep -c "6BgE8R4?HT:+" .env.example` retorna 0
- `grep -c "SUA_" .env.example` retorna >= 8 (pelo menos 8 placeholders)
</acceptance_criteria>

### T02: Verificar .gitignore

<read_first>
- `.gitignore` — verificar regras atuais
</read_first>

<action>
Verificar se `.gitignore` contem:
- `.env`
- `.env.worker`
- `*.log`
- `storage/worker_heartbeat.txt` (opcional mas recomendado)

Adicionar regras faltantes. NAO adicionar `.env.example` ao gitignore (ele DEVE ser commitado, mas com placeholders).
</action>

<acceptance_criteria>
- `.gitignore` contem linha exata `.env`
- `.gitignore` contem padrao `*.log`
</acceptance_criteria>

### T03: Documentar rotacao de credenciais

<read_first>
- `.env.example` — ver placeholders criados
</read_first>

<action>
Criar documento `.planning/phases/01-seguranca/ROTACIONAR-CREDENCIAIS.txt` com:
- Lista de TODOS os servicos que precisam de nova chave (OpenRouter, Evolution API, AbacatePay, SMTP, Google OAuth)
- Instrucoes de onde gerar cada nova chave
- Aviso que as chaves antigas expostas no git DEVEM ser revogadas
- Procedimento: 1) gerar nova chave no painel do servico, 2) atualizar `.env`, 3) revogar chave antiga, 4) testar
</action>

<acceptance_criteria>
- `ROTACIONAR-CREDENCIAIS.txt` existe em `.planning/phases/01-seguranca/`
- Lista OpenRouter, Evolution API, AbacatePay, SMTP, Google OAuth
- Contem instrucoes de rotacao para cada servico
</acceptance_criteria>
